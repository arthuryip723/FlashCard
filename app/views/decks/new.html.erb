<!-- <form action="<%= decks_path %>" method="post">
	<input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">
	<label>Name</label>
	<input type="text" name="deck[name]">
	<button>Submit</button>
</form> -->

<%= form_for @deck do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.submit 'Submit' %>
<% end %>